Copy all included folders into C:\Camera_Surveillance.

Install dashboard dependency:
pip install streamlit

Run dashboard:
streamlit run dashboard\app.py

Run tests:
pytest -v

Run API:
.\scripts\run_api.ps1 -Reload
